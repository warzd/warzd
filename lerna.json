{
  "npmClient": "yarn",
  "packages": ["packages/*"],
  "version": "independent",
  "message": "chore(release): publish %s",
  "command": {
    "publish": {
      "ignoreChanges": ["ignored-file", "*.md", "**/test/**", "**/develop/**"],
      "message": "chore(release): publish %s"
    }
  }
}
